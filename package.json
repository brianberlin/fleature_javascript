{
  "name": "fleature_js",
  "version": "0.0.1",
  "description": "JS client for Fleature.com",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "esbuild index.js --platform=node --bundle --outfile=index.bundled.js",
    "watch": "esbuild index.js --platform=node --watch --bundle --outfile=index.bundled.js"
  },
  "author": "Brian Berlin",
  "license": "ISC",
  "devDependencies": {
    "esbuild-jest": "^0.5.0",
    "esbuild": "^0.14.9",
    "jest": "^27.4.5"
  },
  "dependencies": {
    "tab-election": "^2.0.3"
  }
}